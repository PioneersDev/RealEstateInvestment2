@model RealEstateInvestment.Areas.RealEstate.Models.TypeId
@{
    Layout = null;
}
<div class="panel panel-primary">
    <div class="panel-heading">
        حذف هوية
    </div>
    <div class="panel-body">
        <h3>هل انت متأكد من انك تريد حذف هذه الهوية؟</h3>
        <div>
            <table>
                <tr>
                    <th>
                        @Html.DisplayNameFor(model => model.Id) : &nbsp; &nbsp;
                    </th>

                    <td>
                        @Html.DisplayFor(model => model.Id)
                    </td>
                </tr>
                <tr>
                    <th>
                        @Html.DisplayNameFor(model => model.IdName) : &nbsp; &nbsp;
                    </th>

                    <td>
                        @Html.DisplayFor(model => model.IdName)
                    </td>
                </tr>
            </table>

            @using (Html.BeginForm("Delete", "IdsTypes", FormMethod.Post, new { id = "popupForm" }))
            {
                @Html.AntiForgeryToken()

                <div class="form-actions no-color">
                    <input type="submit" value="حذف" class="btn btn-danger" />
                </div>
            }
        </div>
    </div>
</div>